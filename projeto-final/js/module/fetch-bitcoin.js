// const bitcoinDom = document.querySelector('.bitcoin');
// export default function fetchBtc(url) {
//   fetch(url)
//     .then((r) => r.json())
//     .then((body) => {
//       bitcoinDom.innerText = (`R$ ${body.BRL.buy}`).replace('.', ',');
//       console.log(body.BRL.buy);
//     });

//   setInterval(() => {
//     fetchBtc(url);
//   }, 10000);
// }

// criamos classe somente q uando podemos reaproveitar, quando não podemos, criamos função
